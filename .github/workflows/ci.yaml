name: Test generator

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Test Docker run
      run: |
        docker run --rm --entrypoint /bin/date ubuntu 
